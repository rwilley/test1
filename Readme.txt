this is a test. Git is very difficult for me. I think it is because github must have recently changed the way it works so that now 
git is automatically included in the download and its git shell app functions as git bash did. seems most tutorials do not 
include this info. GIT